# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project overview

WebFlash is a static, browser-based firmware installer for Sense360 ESP32 hubs. The site is a single page that drives ESP Web Tools via Web Serial; there is no application server and no bundler. It is published to GitHub Pages from the repository root.

The codebase has two halves that meet at `manifest.json` — a **publishing pipeline** (Python + GitHub Actions) and a **wizard frontend** (vanilla ES modules). The human-facing explanation of the two-halves model, the `manifest.json` boundary, the desktop-only constraint, the ESP Web Tools standard, the cross-repo contract with `sense360store/esphome-public`, and the deploy gate lives in **[`docs/architecture.md`](docs/architecture.md)** — keep it as the canonical narrative and update it (not this file) when the architecture changes. This section keeps only the AI-actionable constraints.

WebFlash is **downstream** of `sense360store/esphome-public`, which publishes **unsigned** `.bin` artifacts + checksums (WebFlash is the production signing / manifest authority). The cross-repo boundary is exactly three stable surfaces — release **tags**, **config-string** values, and **artifact names**; upstream's internal board/bundle/alias/shim YAML layering is invisible to WebFlash, and no WebFlash file references any upstream `packages/` or `products/` path. Do not couple WebFlash to upstream internals — keep `firmware/sources.json`, `manifest.json`, and `scripts/data/` pinned to config strings and artifact names only. See [`docs/architecture.md`](docs/architecture.md) → *Cross-repo contract* for the full narrative and the `WEBFLASH-ARCH-SYNC-001` no-drift re-audit in [`docs/product-import-readiness.md`](docs/product-import-readiness.md).

### Platform and standards

- **Follows the ESP Web Tools / esptool.js standard** for flashing ESP32 devices. The wizard renders the upstream `<esp-web-install-button>` component (loaded from unpkg) and consumes the standard ESP Web Tools manifest schema (`name`, `version`, `builds[].chipFamily`, `builds[].parts[].path`/`offset`, `improv`, etc.). Do not invent custom flash flows — extend behavior via the documented overrides surface (e.g. `checkSameFirmware` in `scripts/utils/esp-web-tools-overrides.js`) so the upstream component still drives connect/erase/write/verify.
- **Laptop / desktop only.** Web Serial is not available on iOS, Android Chrome, or any mobile browser, so WebFlash explicitly targets desktop Chromium-based browsers (Chrome, Edge, Opera) on Windows / macOS / Linux. Firefox and Safari are unsupported. Capability detection lives in `scripts/capabilities.js` and surfaces an unsupported-browser banner via `scripts/init-review.js`. Do not add mobile-first layout assumptions or features that imply mobile is a supported runtime — the install path will not work there.

## Sense360 hardware reference (canonical SKUs)

This is the **authoritative SKU list** for the supported hardware. The **Friendly name** column is the canonical user-facing label — use it verbatim in wizard markup, manifest descriptions, and module metadata. There is no Model/Variant axis: each SKU is its own product, and "Base / Pro" or model/variant terminology must be dropped when touching this code. The **Old name** column lists deprecated internal/historical names and exists only to help recognise legacy references; do not use these in new code.

| Group | Type | Friendly name | SKU | Rev | Old name | What it does |
|---|---|---|---|---|---|---|
| Ceiling | Hub | Sense360 Core | S360-100 | R4 | `360Core_Ceiling_V3_R` | Main board. Has the ESP32-S3 and connectors for all other modules. |
| Ceiling | Sensor | Sense360 RoomIQ | S360-200 | R4 | Presence + Comfort (two boards) | Merged board. PIR, LD2450, SEN0609, LTR-303ALS (light), SHT4x (temp and humidity), BMP581 (pressure). |
| Ceiling | Sensor | Sense360 AirIQ | S360-210 | R4 | `AirlQ Ceiling` (typo in old name) | Air quality board. CO2 (SCD41), VOC (SGP41), gas (MICS-4514 with STM8). Connectors for PM (SPS30) and HCHO (SFA30). |
| Ceiling | Sensor | Sense360 VentIQ | S360-211 | R4 | Bathroom Pro | Smaller air quality board for bathrooms. SGP41 on board. Connectors for IR temp and SPS30. |
| Ceiling | Indicator | Sense360 LED | S360-300 | R4 | LED Ring | Ring of WS2812B LEDs. |
| Inline | Driver | Sense360 Relay | S360-310 | R4 | `S360-Relay-C`, `Sense360 Fan Relay` | On / off relay for bathroom fans. |
| Inline | Driver | Sense360 PWM | S360-311 | R4 | `12vFan_PWM_PulseCounter`, `Sense360 Fan PWM` | 12V PWM fan driver, up to 4 fans with tach feedback. |
| Inline | Driver | Sense360 DAC | S360-312 | R4 | `Fan_GP8403`, `Sense360 Fan DAC` | 0 to 10V analog fan driver, for example Cloudlift S12. |
| Inline | Driver | Sense360 TRIAC | S360-320 | R4 | `TRIAC_Board` | Phase dimmer for mains fan or lamp. |
| Power | PSU | Sense360 240v PSU | S360-400 | R4 | PWR Module, `Sense360 Mains PSU` | 240V mains to 5V using HLK-5M05. |
| Power | PSU | Sense360 PoE PSU | S360-410 | R4 | PoE Module | PoE to 5V. |

Notes:

- The current wizard exposes Ceiling mount only; a "Wall" branch lingers in markup/legacy aliases but is not a supported product.
- **Wizard coverage of the table:** the sensor/driver/indicator SKUs are *separately selectable* via `scripts/data/module-requirements.js` (RoomIQ S360-200, AirIQ S360-210, VentIQ S360-211, LED S360-300, Relay S360-310, PWM S360-311, DAC S360-312, TRIAC S360-320). Nothing is bundled — each SKU is its own product, and the user picks every module they have. The Core (S360-100) is the one exception: it is implicit because every flashable device is a Core. The 240v PSU (S360-400) and PoE PSU (S360-410) are surfaced through the `power` selection (`pwr` / `poe`) rather than as their own module entries. When introducing a new *selectable* SKU, add it to `module-requirements.js` and update the wizard SKU labels in `state.js` (`MODULE_LABELS`, `MODULE_VARIANT_LABELS`, `MODULE_SEGMENT_FORMATTERS`) — do not regress to model/variant nomenclature, and do not describe any SKU as "bundled".

## Commands

```bash
# Tests (Jest with experimental ESM VM modules — required because the codebase is pure ESM)
npm test
npm test -- url-config                  # filter by name
npm test -- __tests__/wizard-state.test.js
npm test -- --watch

# Naming-policy validator (also runs in CI before manifest generation)
npm run validate:naming-policy
# or directly:
node scripts/validate-naming-policy.js firmware/configurations

# Regenerate manifest.json + firmware-*.json after adding/removing firmware
python3 scripts/gen-manifests.py --summary
python3 scripts/gen-manifests.py --summary --dry-run    # preview without writing

# Local dev server (no build step — open in Chrome/Edge/Opera; Web Serial is required)
python3 -m http.server 5000
```

There is no lint or typecheck step. CI runs `npm test -- --ci` with `continue-on-error: true` (the suite is being cleaned up — do not skip hooks/flags to bypass). The Python publishing scripts have no test suite.

## Architecture

### Wizard frontend (entry: `app.js`)

`app.js` imports each wizard module exactly once and registers the service worker. The order in `app.js` matters — `state.js` must load before modules that read state, and `error-log.js` is imported early via `state.js` to capture manifest-load failures.

`scripts/state.js` (~5000 lines) is the **central state module** and the source of truth. It owns:

- The wizard configuration object (`mounting`, `power`, `bathroom`, plus module keys `voice`, `led`, `airiq`, `fan`, `ventiq`).
- Step gating via `getMaxReachableStep()` — step 2 unlocks once `mounting` is set, step 3 once `power` is set, etc.
- Manifest loading, parsing of `config_string` values like `"Ceiling-POE-VentIQ-RoomIQ"` back into wizard state (`parseConfigStringState`), and matching builds to the current selection.
- The Step 5 preflight engine (`evaluatePreflightPolicy`) and connection-quality metrics fed by `navigator.serial` connect/disconnect events and ESP Web Tools `state-changed` events.
- All install/download gating: install only fires when no preflight `Fail` exists, the **Before you flash** checkbox is checked, and any `Warning` is acknowledged.

It exports a small surface (`getState`, `setState`, `replaceState`, `getStep`, `setStep`, `getMaxReachableStep`, `getTotalSteps`) and a `__testHooks` bundle used exclusively by Jest.

Other notable pieces:

- `scripts/data/module-requirements.js` — hardware compatibility matrix (SKUs, headers, conflicts, `recommended`/`ceilingOnly`/`requiresBathroom` flags). **Constraint enforcement reads from this file**; keep it consistent with the canonical SKU table above and with the README option tables.
- `scripts/utils/url-config.js` — bidirectional parser for sharable config URLs. Maintains legacy aliases (e.g. `pwr` → `ac`, `BathroomAirIQ*` → `VentIQ`, fan `pwm` ↔ `base`) so old links still resolve. The wizard URL key `voice` historically maps to `core` in the URL alias set.
- `scripts/utils/esp-web-tools-overrides.js` — installs a `MutationObserver` that attaches a `checkSameFirmware` override to every `<esp-web-install-button>` so users see a warning when reflashing the same version reported via Improv.
- `scripts/recommended-bundle.js` — quick-start preset application; uses `getMaxReachableStep` to jump straight to step 4 when applying a preset.
- `scripts/utils/preset-storage.js` — JSON preset import/export with schema versioning; deprecated keys (`presence`, `comfort`) are stripped on read.
- `scripts/utils/flash-history.js` — flash attempts logged to `localStorage` for diagnostics; entries also strip deprecated keys.
- `sw.js` — service worker. Strategy is network-first for `*.bin` and `manifest.json`, stale-while-revalidate for everything else. **When you add new top-level scripts, add them to `STATIC_ASSETS` or `SCRIPT_MODULES` in `sw.js`** or they will not be available offline.
- `scripts/wizard-state-observer.js` — `window.WizardState` legacy observer that infers state from the DOM via a `MutationObserver`. Newer code reads from `state.js` directly; this exists for older inspector code that watches the DOM.

### Publishing pipeline

`scripts/gen-manifests.py` is the only way `manifest.json` and `firmware-*.json` should change — these files are **generated, not hand-edited**. It scans `firmware/`, parses each filename via the canonical pattern (see below), produces a single `manifest.json` with full per-build metadata (including hashes and a `config_string` like `Ceiling-POE-VentIQ-RoomIQ`), and writes one `firmware-<index>.json` per build (the standard ESP Web Tools per-product manifest format). The generated manifest must match the actual `.bin` files on disk — the `__tests__/manifest-health.test.js` guard (WF-CLEANUP-006) fails CI before deploy if any `manifest.json` or `firmware-*.json` build references a missing `.bin`, if a `firmware/configurations/*.bin` lacks its `.meta.json` sidecar, if the per-build manifests drift out of sync with `manifest.json`, if a blocked token (`FanTRIAC` globally, plus any `block_tokens` declared for a matching source in `firmware/sources.json`) reappears in a `config_string`, or if a `REQUIRED_CONFIGS` entry is missing from `manifest.json`.

New firmware that is meant to ship enters the tree through the cross-repo importer, not through hand-copying a `.bin` into `firmware/configurations/`. Declare the source in [`firmware/sources.json`](firmware/sources.json) and run [`scripts/import-firmware-sources.py`](scripts/import-firmware-sources.py) (or dispatch `.github/workflows/firmware-import.yml`) — the importer fetches the upstream `.bin` from `sense360store/esphome-public`, verifies its SHA256 against the upstream `checksums-sha256.txt`, enforces the per-source `block_tokens` allowlist, and writes the `<asset>.meta.json` sidecar. The Rescue firmware (built in-tree under `firmware/rescue/`) is the only sanctioned exception. See [`docs/firmware-import.md`](docs/firmware-import.md) for the full contract.

The Python script still carries a legacy `model` / `variant` code path for binaries placed outside `firmware/configurations/`, and `scripts/compat-config.js` keeps a matching frontend lookup (`createModelSignature`, `lookup.type === 'model'`) for legacy share-links that arrive with `?model=…&variant=…` parameters. **Treat both as deprecated** — do not introduce new firmware down that branch and do not extend Model/Variant metadata in new code. New SKUs and configurations belong in `firmware/configurations/` with the canonical `Sense360-...-vX.Y.Z-<channel>.bin` filename, identified by SKU/config-string only.

`scripts/validate-naming-policy.js` enforces:

- Canonical filename shape `Sense360-...-vX.Y.Z-(stable|preview|beta).(bin|md)`.
- Disallowed token migrations: `AirIQProv` → `AirIQPro`, `AirIQBase` → `AirIQ`, `BathroomAirIQ` → `Bathroom`, `FanAnalog` → `FanDAC`. Fan variants are now preserved as variant-specific tokens (`FanRelay`, `FanPWM`, `FanDAC`, `FanTRIAC`) so each driver SKU lands on a different firmware binary; the legacy generic `Fan` token must not be used in new firmware.
- Channel placement: only `*-stable.md` is allowed under `firmware/configurations/`. Preview/beta/dev release notes belong in `firmware/previews/`.

`.github/workflows/firmware-publish.yml` runs unit tests, the naming-policy validator, the manifest generator, and a `REQUIRED_CONFIGS` allowlist that fails the build if any of the expected `config_string` values are missing from `manifest.json`. The live array in the workflow file is the source of truth. **As of WF-CLEANUP-004 the allowlist holds exactly 2 entries — `Ceiling-POE-VentIQ-RoomIQ` (current Release-One, imported from `sense360store/esphome-public` v1.0.0) and `Rescue`** — i.e. the configs WebFlash can actually ship today, each backed by a real signed `.bin` on disk. The 8 legacy entries that used to live here (`Ceiling-POE-AirIQ`, `Ceiling-POE-VentIQ`, `Ceiling-PWR-AirIQ`, `Ceiling-USB`, `Ceiling-USB-AirIQ`, `Ceiling-USB-FanPWM`, `Ceiling-Voice-POE-AirIQ`, `Ceiling-Voice-USB`) were removed because none had a backing `.bin` or a `firmware/sources.json` source entry. Do **not** re-add any legacy `config_string` to `REQUIRED_CONFIGS` until a corresponding source entry has been imported via `scripts/import-firmware-sources.py`, the `.bin` + `.meta.json` sidecar are on disk, and the regenerated manifest reflects the new build.

Two related invariants travel with the allowlist and must not be regressed:

- **FanTRIAC stays blocked from Release-One.** `firmware/sources.json` carries `block_tokens: ["FanTRIAC", "LED"]` on the v1.0.0 source entry, the importer refuses to ingest a FanTRIAC-bearing asset, and the manifest-health guard fails CI if a `FanTRIAC` token ever reappears in a generated `config_string`. The orphan `Sense360-Ceiling-POE-VentIQ-FanTRIAC-RoomIQ-v1.0.0-stable.bin` was removed by WF-CLEANUP-002. Do not re-add FanTRIAC to Release-One or to `REQUIRED_CONFIGS` until the S360-320 hardware verification work lands. (FanTRIAC stays a *permitted filename token* for the naming-policy validator and a real SKU in the hardware table; the block is enforced at import + manifest-health time, not at the filename layer.)
- **LED stays excluded from Release-One.** Same `block_tokens` mechanism. Do not introduce an `LED` token into Release-One's `config_string` unless the source declaration is updated deliberately and a separate LED build is imported.

WF-PRODUCT-001 adds `__tests__/product-catalog-alignment.test.js` (with a vendored snapshot at `__tests__/fixtures/esphome-product-catalog.json`) which cross-checks `firmware/sources.json`, `manifest.json`, every `firmware-*.json`, the workflow's `REQUIRED_CONFIGS`, and `scripts/data/kits.json` against the upstream `sense360store/esphome-public` product lifecycle catalog. WebFlash-eligible statuses are `production` (required for `REQUIRED_CONFIGS`) and `preview` (also accepted for sources, manifests, and kits — relaxed from production-only by WF-LED-002 so the LED preview source can land alongside Release-One); `blocked`, `legacy-compatible`, `deprecated`, `removed`, `hardware-pending`, and `compile-only` all fail. `Rescue` is exempt by name. The fixture is the offline default; set `PRODUCT_CATALOG_PATH` to validate against a freshly downloaded upstream catalog. WF-PRODUCT-002 refreshed that fixture against the current upstream state — the upstream snapshot at refresh time held 33 products (1 production = `Ceiling-POE-VentIQ-RoomIQ`, 1 blocked = `Ceiling-POE-VentIQ-FanTRIAC-RoomIQ`, 0 preview, 31 legacy-compatible enumerated by upstream PRODUCT-002), no status WebFlash mirrors changed, no LED-bearing product appeared upstream, active WebFlash surfaces still resolve only to Release-One + Rescue, and FanTRIAC remains blocked / LED remains excluded from Release-One. WF-PRODUCT-003 refreshed the fixture again after upstream PRODUCT-009 promoted an LED-bearing sibling product (`Ceiling-POE-VentIQ-RoomIQ-LED`, `status: preview`, `channel: preview`, `version: 1.0.0`, `artifact_name: Sense360-Ceiling-POE-VentIQ-RoomIQ-LED-v1.0.0-preview.bin`, `webflash_build_matrix: true`) — the upstream snapshot now holds 34 products (1 production / 1 preview / 1 blocked / 31 legacy-compatible), the WF-PRODUCT-002 synthetic preview placeholder was removed and replaced with the real upstream LED preview row, and the alignment test gained an explicit `WF-PRODUCT-003 — upstream LED preview recognition` describe block that pins both halves of the awareness-but-non-exposure contract. WF-LED-001 is a docs-only follow-up that authors [`docs/led-preview-import-plan.md`](docs/led-preview-import-plan.md) capturing the future LED preview import shape — the upstream proof fields required before WebFlash may import, the future `firmware/sources.json` source entry's `block_tokens: ["FanTRIAC"]` (Release-One's source keeps `["FanTRIAC", "LED"]`), the import + manifest-regeneration sequence, the deferred UX / kit decisions, and the do-not-change list. WF-LED-001 imports no firmware, regenerates no manifests, modifies no `firmware/sources.json`, changes no `REQUIRED_CONFIGS`, and adds no LED UI or kit. WF-LED-002 then performs the import: upstream [`v1.0.0-led-preview`](https://github.com/sense360store/esphome-public/releases/tag/v1.0.0-led-preview) shipped a proven LED preview artifact (SHA256 `93310d2cbc27355e399f36a232336b6b9075dacfc178d603c7a92aa1089182d3`, 1,135,904 bytes, release body with all four canonical H2 sections), and WF-LED-002 added a second `firmware/sources.json` entry (`channel: preview`, `block_tokens: ["FanTRIAC"]`, pinned `expected_sha256`), imported the `.bin` + `.meta.json` sidecar, regenerated `manifest.json` (now 3 builds — Release-One stable + LED preview + Rescue) and the per-build manifests (`firmware-0.json` = Release-One, `firmware-1.json` = LED preview, `firmware-2.json` = Rescue after deterministic re-indexing). WF-LED-002 also hardened `scripts/import-firmware-sources.py` to enforce `expected_sha256` when present (backward compatible when absent). Unchanged by WF-LED-002: Release-One source entry, Release-One manifest build content, Rescue build content, `REQUIRED_CONFIGS` (still `["Ceiling-POE-VentIQ-RoomIQ", "Rescue"]` — production-only), `scripts/data/kits.json` (still Release-One-only), every UI / wizard / `sw.js` / `index.html` / workflow file, and FanTRIAC blocked status under HW-005. The `WF-PRODUCT-003 — upstream LED preview recognition` describe block now asserts LED preview presence in `firmware/sources.json` + `manifest.json` and absence in `REQUIRED_CONFIGS` + `scripts/data/kits.json`. WF-LED-003 then resolved the deferred UX call with **Option A — manifest-only preview**: no new kit, no new mode toggle, no wizard / service-worker / workflow / signing / firmware / manifest / `firmware/sources.json` change. The exposure mechanism is the existing release-channel gate in `scripts/utils/release-channels.js` (`preview.defaultSelectable: false` so the LED build is never auto-selected even as the sole candidate, `preview.requiresAcknowledgement: true` so install gates on a `channel:preview` checkbox with experimental-build warning copy, `preview.hiddenByDefault: false` so the build is visible in normal mode without a `?mode=preview` toggle — `state.js`'s `VALID_RELEASE_MODES` stays `normal` / `recovery` / `development`) combined with the existing LED module toggle in step 4 of the wizard (`Sense360 LED` toggle in `index.html`, `led` module key wired into `MODULE_KEYS` / `MODULE_SEGMENT_FORMATTERS` / `parseConfigStringState` in `scripts/state.js`, and the `Sense360 LED` (S360-300) variant entry in `scripts/data/module-requirements.js`). With the LED toggle off the stable Release-One install path is byte-identical to pre-WF-LED-002; with the LED toggle on the wizard produces `config_string: Ceiling-POE-VentIQ-RoomIQ-LED` and resolves to the preview build behind the existing preview gate. WF-LED-003 added a targeted policy-level test in `__tests__/release-channel-ui.test.js` (`WF-LED-003 — LED preview exposure model …` describe block) that pins the LED-preview-shaped build against the exposure model: never auto-selected by `pickDefaultBuild`, stable wins when both are candidates, `channel:preview` acknowledgement required, visible in normal mode, Preview badge with warning tone, never tagged Recommended. A future WF-LED-004 may revisit the UX surface only after **either** upstream promotes the LED catalog entry to `status: production` (which would unblock a `REQUIRED_CONFIGS` entry) **or** S360-300 bench verification clears the LED hardware path (which would justify a preview-labelled kit or a dedicated preview-channel control); neither precondition has landed as of WF-LED-003. WF-PRODUCT-004 then adds an advisory readiness validator at [`scripts/validate-product-import-readiness.js`](scripts/validate-product-import-readiness.js) with the contract doc at [`docs/product-import-readiness.md`](docs/product-import-readiness.md). The validator classifies every upstream catalog entry against four independent dimensions — **import-eligible**, **manifest-eligible**, **`REQUIRED_CONFIGS`-eligible**, **kit-eligible** — and cross-checks the live WebFlash surfaces (`firmware/sources.json`, `manifest.json`, `REQUIRED_CONFIGS`, `scripts/data/kits.json`) against the catalog lifecycle. `production`+`stable` is the only state that may enter `REQUIRED_CONFIGS`; `preview` is import / manifest / kit eligible but never `REQUIRED_CONFIGS` eligible; `blocked` / `legacy-compatible` / `deprecated` / `removed` / `hardware-pending` / `compile-only` never qualify; `Rescue` is exempt by name. Today's classifications are unchanged by WF-PRODUCT-004: Release-One (`Ceiling-POE-VentIQ-RoomIQ`) is import + manifest + `REQUIRED_CONFIGS` + kit eligible; the LED preview (`Ceiling-POE-VentIQ-RoomIQ-LED`) is import + manifest + kit eligible but **not** `REQUIRED_CONFIGS` eligible; the FanTRIAC blocked entry is ineligible everywhere; the legacy-compatible representative is ineligible everywhere. WF-PRODUCT-004 is reporting-only — it does not import firmware, regenerate manifests, change `REQUIRED_CONFIGS`, add kits, modify any UI / wizard / `sw.js` / workflow surface, or alter `__tests__/fixtures/esphome-product-catalog.json`. Defaults to Markdown output against the vendored fixture; supports `--catalog <path>`, `--config <config_string>`, `--format markdown|json`; exits 0 on consistent classification, 1 on violation, 2 on usage error. The Jest pin at [`__tests__/product-import-readiness.test.js`](__tests__/product-import-readiness.test.js) (run via `npm run test:product-import-readiness`) locks the rules and the current fixture classifications.

### Frontend ↔ pipeline contract

The wizard's selection is reduced to a `config_string` (e.g. `Ceiling-POE-VentIQ-RoomIQ`) and matched against `build.config_string` in `manifest.json`. `parseConfigStringState` in `state.js` and the canonical token formatters in `MODULE_SEGMENT_FORMATTERS` define how segments encode/decode (`AirIQ` → `airiq=airiq`, `VentIQ` → `ventiq=airiq`, `FanRelay` → `fan=relay`, `FanPWM` → `fan=pwm`, `FanDAC` → `fan=analog`, `FanTRIAC` → `fan=triac`, etc.). When you add a new module token, update both:

1. The wizard's segment formatter and `parseConfigStringState` in `scripts/state.js`.
2. `CANONICAL_MODULE_TOKENS` / token-handling logic in `scripts/gen-manifests.py`.

Otherwise the frontend will fail to find a build that the manifest claims exists.

## Conventions and gotchas

- **Desktop / Web Serial only.** The install path depends on `navigator.serial`, which is only implemented on desktop Chromium browsers. Do not add code paths that assume mobile or non-Chromium browsers can flash; gate any new install-time UI behind the existing capability detection in `scripts/capabilities.js` and surface unsupported-browser messaging through `init-review.js`.
- **Pure ESM.** Tests require `NODE_OPTIONS=--experimental-vm-modules` (already set by `npm test`). Do not introduce CommonJS modules under `scripts/` or in tests; new tests should use `import { ... } from '@jest/globals'`. Jest config (`jest.config.cjs`) sets `transform: {}` — no transpilation.
- **No external runtime dependencies in the wizard.** The only third-party script loaded by `index.html` is `esp-web-tools` from unpkg, which is allowed by the `Content-Security-Policy` in `_headers`. If you need new origins (scripts, fonts, connect-src), update the CSP there.
- **`_headers` is GitHub-Pages-style.** It controls CORS, CSP, and cache rules. Firmware binaries are served with `Cache-Control: max-age=31536000`, so versioned filenames are critical — never overwrite a published `.bin` in place.
- **Disabled options live in the matrix, not in markup.** The canonical SKU table above documents the products; runtime gating comes from `module-requirements.js` (e.g. `ceilingOnly`, `requiresBathroom`) and the visibility logic in `getVisibleModuleGroupKeys` in `state.js`. AirIQ ↔ VentIQ is mutually exclusive and driven by the Bathroom toggle on Ceiling mounts.
- **No Model/Variant axis.** The product taxonomy is flat (one SKU per product). When extending the wizard, do not add Base/Pro variants or model/variant fields; add a new SKU entry to `module-requirements.js` and a new module key to `MODULE_KEYS` in `state.js` instead.
- **Sensitive-value redaction.** `Copy diagnostics` and flash history both pass through redaction (`SENSITIVE_KEY_PATTERN` in `state.js`, `stripDeprecatedConfigurationFields` in `flash-history.js`). When adding new fields to diagnostics or history, audit whether they should be redacted before they ship.
- **Service worker cache name is `webflash-v1`.** Bumping the cache version (or the `?v=` query in `index.html`'s stylesheet links) is how forced refreshes are landed; the `activate` handler deletes any cache that starts with `webflash-` but is not the current name.
- **Generated files are committed.** `manifest.json`, every `firmware-*.json`, and every `firmware/configurations/*.bin` are tracked in git. Regenerate with `gen-manifests.py` and commit the diff together with the firmware change in the same commit.
- **Branch policy.** All AI-assisted development on this repo runs on a dedicated `claude/...` branch (see workflow instructions). Never push to `main` directly.
- **Upcoming PR queue.** [`UPCOMING_PR.md`](UPCOMING_PR.md) is the working queue tracker for completed, blocked, deferred, and upcoming WebFlash PRs. Every WebFlash PR that changes queue state must update it; upstream `sense360store/esphome-public` rows appear only as dependencies.
- **Wizard UX roadmap.** Live-wizard UX audit, severity-classified findings, target first-run flow, and PR sequencing for `WF-UX-QUICK-001` through `WF-UX-007` (plus the operator-only `WF-HW-TEST-001` / `WF-HW-TEST-002` chain) live in [`docs/wizard-ux-roadmap.md`](docs/wizard-ux-roadmap.md). Reference it when proposing UX-shaped changes; WF-LED-003's preview-channel exposure model and the FanTRIAC block must round-trip through that roadmap's do-not-change guardrails. The operator-validation container for the LED preview flash path lives at [`docs/led-preview-webflash-proof.md`](docs/led-preview-webflash-proof.md) (status: **pending — operator hardware test required**). WF-HW-TEST-001 captured the pre-flight live-deployment evidence and operator procedure; WF-HW-TEST-002 was the planned operator-evidence-collection follow-up but **no operator evidence was supplied**, so every proof row stays `pending` and no row was flipped to a recorded outcome by WF-HW-TEST-002. The LED preview channel, FanTRIAC blocked status, production-only `REQUIRED_CONFIGS`, Release-One-only `scripts/data/kits.json`, manifest / sources / firmware / `sw.js` / workflow surfaces are all byte-identical to pre-WF-HW-TEST-002, and `S360-300-BENCH-001` / RELEASE-007 remain separate gates that WF-HW-TEST-002 does **not** satisfy.
- **Module availability gating (WF-WIZARD-AVAIL-001, amended by WF-TRIAC-001).** Step 4 carries a presentation-only availability classifier at [`scripts/utils/module-availability.js`](scripts/utils/module-availability.js) that pins every module variant to one of **eight** states (`available-stable`, `available-preview`, `no-firmware`, `design-pending`, `blocked`, `advanced-manual-warning`, `legacy-only`, `hidden`). The classifier reads from the loaded manifest's `config_string` / `channel` set (passed in by `state.js` via `deriveManifestIndex`) and from the static per-variant overrides in the helper itself. **Static overrides take precedence** so the wizard can encode reasons the manifest cannot carry on its own: AirIQ (S360-210) is **no longer** a static override — WF-PREVIEW-IMPORT-FIRST-BATCH-001 imported a `Ceiling-POE-AirIQ-RoomIQ` preview build, so AirIQ now derives `available-preview` straight from the manifest (like LED); Relay (S360-310) → `design-pending` because no S360-310 schematic has been uploaded upstream; PWM (S360-311) and DAC (S360-312) → `no-firmware` because the S360-311-R4 / S360-312-R4 schematics live in upstream `sense360store/esphome-public` but no WebFlash build ships; TRIAC (S360-320) → `advanced-manual-warning` under HW-005 + COMPLIANCE-001 regardless of the S360-320-R4 schematic upload (per WF-TRIAC-001 — see the dedicated bullet below); Voice → `legacy-only` (the `voice: 'none'` state stays as the Core placeholder for share-link back-compat). LED is derived (currently `available-preview`) so a future stable promotion lights up automatically without code changes. The helper is **not** the install gate — `release-channels.js` (preview acknowledgement), the WF-TRIAC-001 advanced/manual-warning acknowledgement, provenance, freshness, and preflight stay authoritative. The classifier surfaces through per-card pills + `data-availability-state` data hooks; the WF-UX-002 `firmware-readiness.js` body copy is unchanged. The `__tests__/module-availability.test.js` pin locks the classifier; `__tests__/wizard-state.test.js` `WF-WIZARD-AVAIL-001 — module availability runtime integration` pins the runtime rendering; `__tests__/a11y-static-html.test.js` `WF-WIZARD-AVAIL-001 — module availability pill slots …` pins the static markup contract.
- **Step 1 kit / custom / recovery path split (WF-UX-006).** Step 1 exposes three customer-facing path cards — *I bought a kit* / *Custom configuration* / *Recovery* — via the static `[data-start-path]` button group in [`index.html`](index.html) (replacing the legacy binary `[data-config-mode-picker]` radio set, which survives as a hidden back-compat surface labelled `.config-mode-picker--legacy` so the existing kit-mode change-listener and older share-links keep parsing). The kit card reveals the existing `[data-kit-mode-panel]` (catalogue dropdown, summary card, and the relocated "Most popular" preset card as a no-SKU fallback). The custom card reveals a new `[data-custom-path-panel]` (which keeps `[data-manual-mode-panel]` as a back-compat alias) and advances through the normal 5-step wizard via the existing `[data-next]` button. The recovery card carries `data-rescue-open` so the existing rescue-modal delegated click handler in [`scripts/layout/rescue-modal.js`](scripts/layout/rescue-modal.js) opens the dialog — rescue firmware, the rescue manifest at `firmware/rescue/manifest.json`, and the modal install logic are untouched. Path state lives in [`scripts/kit-mode.js`](scripts/kit-mode.js): `setPath('kit'|'custom'|'recovery')` layers on top of the existing `setMode('kit'|'manual')`, with `getCurrentPath` exposed via `__testHooks`. **The path value is presentation-only** — it never enters `config_string`, `manifest.json`, `firmware-*.json`, `firmware/sources.json`, `scripts/data/kits.json`, `scripts/utils/release-channels.js`, the install gate, the freshness/preflight engines, or any WebFlash workflow. URL contract uses the existing `configmode=` namespace (`configmode=kit`, `configmode=kit&sku=…`, `configmode=custom`, with `configmode=manual` accepted as a back-compat alias); no new `path=` URL key was introduced. Kit-path end-to-end resolves only to the Release-One config (`Ceiling-POE-VentIQ-RoomIQ`) — `state.led === 'none'`, `state.airiq === 'none'`, every fan variant `'none'`, Voice `'none'`. Custom-path keeps every WF-WIZARD-AVAIL-001 availability pill (RoomIQ + VentIQ available-stable, AirIQ + LED available-preview after WF-PREVIEW-IMPORT-FIRST-BATCH-001, PWM + DAC no-firmware, Relay design-pending, TRIAC advanced-manual-warning). Test pins: `__tests__/a11y-static-html.test.js` `WF-UX-006 — Step 1 kit / custom / recovery path split` (static markup contract), `__tests__/kit-mode.test.js` `WF-UX-006 — Step 1 path selector` (path controller + URL hydration + rescue-modal delegation), `__tests__/wizard-state.test.js` `WF-UX-006 — kit path resolves only to Release-One` + `WF-UX-006 — custom path preserves unavailable-module honesty` (kit + custom invariants end-to-end). **Unchanged by WF-UX-006:** every firmware binary, `manifest.json`, every `firmware-*.json`, `firmware/sources.json`, `scripts/data/kits.json`, `scripts/utils/release-channels.js`, `scripts/utils/firmware-readiness.js`, `scripts/utils/module-availability.js`, `sw.js`, every `.github/workflows/*` file, the rescue modal, the rescue manifest, the `REQUIRED_CONFIGS` allowlist, the WF-LED-003 preview-channel acknowledgement model, the FanTRIAC HW-005 block, the Voice quarantine, and the WF-UX-005 step labels.
- **Step 4 jargon and module-label cleanup (WF-UX-007).** Step 4 module cards and toggle groups carry **outcome-first** primary titles — `Room sensing` (S360-200 RoomIQ), `Air quality sensing` (S360-210 AirIQ), `Bathroom air sensing` (S360-211 VentIQ), `Status LED ring` (S360-300 LED), `Fan and switching control` (group H3), `Fan relay control` (S360-310 Relay), `PWM fan control` (S360-311 PWM), `Analog fan control` (S360-312 DAC), `TRIAC fan control` (S360-320 TRIAC) — with the technical Friendly Name + SKU layered into a secondary `.module-group__meta` / `.module-card__meta` line (`Sense360 RoomIQ · S360-200`, etc.). Pin/header / Core-rev technical detail stays in the existing `.module-card__specs` definition list (third tier). The Fan group H3 + chevron `aria-label` swap is static-markup only — `MODULE_LABELS` in [`scripts/state.js`](scripts/state.js) still resolves the fan group to `"Fan / Switching"` so the dynamic Step 4 summary line, diagnostics, firmware-not-available, and `firmware-nearest.js` keep the locked technical label (`__tests__/diagnostics-bundle.test.js`, `__tests__/firmware-not-available.test.js`, `__tests__/state-summary-modules.test.js`, `__tests__/state-config-string.test.js` all unchanged and green). The orphan `airiq.base` / `airiq.pro` entries in [`scripts/content/option-tooltips.js`](scripts/content/option-tooltips.js) are removed; `airiq.none` stays because the popover lookup falls through to `null` cleanly. Test pins: `__tests__/a11y-static-html.test.js` `WF-UX-007 — outcome-first Step 4 module labels with technical secondary tier` (static markup contract for the new outcome + meta tiers and the fan H3 / aria-label swap), `__tests__/module-selection-guidance.test.js` `WF-UX-007 — Step 4 primary labels are outcome-first; technical names move to the secondary tier` (primary-vs-secondary tier separation + locked bathroom / pairing / firmware-target / fan variant warning copy), `__tests__/option-tooltips.test.js` (orphan removal + Base/Pro nomenclature absence). **Unchanged by WF-UX-007:** config-string grammar, manifest matching, kit matching, URL aliases, release channels, preview acknowledgement, module availability states, TRIAC HW-005 block, Voice quarantine, Release-One stable path, LED preview path, firmware-target hint copy, bathroom toggle copy, fan variant warning copy, conflict badge copy, diagnostics / support technical labels; every firmware binary, `manifest.json`, every `firmware-*.json`, `firmware/sources.json`, `scripts/data/kits.json`, `scripts/data/module-requirements.js` field values, `scripts/state.js` `MODULE_KEYS` / `MODULE_LABELS` / `MODULE_VARIANT_LABELS` / `MODULE_SEGMENT_FORMATTERS` / `parseConfigStringState`, `scripts/layout/state-summary.js` FIELD_MAP, `scripts/recommended-bundle.js`, `scripts/kit-mode.js`, `scripts/utils/release-channels.js`, `scripts/utils/firmware-readiness.js`, `scripts/utils/module-availability.js`, `sw.js`, `_headers`, every `.github/workflows/*` file, and the kit summary card at [`index.html:258-260`](index.html#L258).
- **WebFlash import readiness matrix (WF-IMPORT-GAP-001).** Future firmware imports must round-trip through the WebFlash-side import readiness matrix at [`docs/webflash-import-readiness-matrix.md`](docs/webflash-import-readiness-matrix.md), which sits in front of the import mechanism in [`docs/firmware-import.md`](docs/firmware-import.md) and the WF-PRODUCT-004 catalog eligibility validator in [`docs/product-import-readiness.md`](docs/product-import-readiness.md). The matrix classifies every candidate import family (Relay / PWM / DAC / TRIAC / 240V PSU / PoE PSU / LED stable / AirIQ) against seven import classes (`none`, `docs-only / manual YAML only`, `preview import candidate`, `advanced / manual-warning import only`, `stable import candidate after promotion`, `stable import`, `rescue import`, `legacy-only`) and reserves the deliberate per-family follow-up PR identifiers — `WF-IMPORT-RELAY-001` after `RELEASE-RELAY-001`, `WF-IMPORT-PWM-001` after `RELEASE-PWM-001`, `WF-IMPORT-DAC-001` after `RELEASE-DAC-001`, `WF-IMPORT-TRIAC-001` after `RELEASE-TRIAC-001` + `WF-TRIAC-001` runtime UX, `WF-IMPORT-POWER-400-001` after `RELEASE-POWER-400-001`, `WF-IMPORT-POE-410-001` reserved (no separate PoE-PSU import needed today because PoE is already covered transitively by the Release-One + LED preview `power=poe` artifacts), `WF-LED-STABLE-001` after `RELEASE-007` + `S360-300-BENCH-001`, plus the orthogonal `WF-REQUIRED-001` for any `REQUIRED_CONFIGS` decision and `WF-KIT-LED-001` for any LED kit / recommended decision. Four separation invariants travel with every row: *release artifact existence does not mean WebFlash import*, *WebFlash import does not mean `REQUIRED_CONFIGS`*, *WebFlash import does not mean kit / recommended / default exposure*, *advanced / manual-warning import is not compliance certification*. WF-IMPORT-GAP-001 itself is documentation-only — Release-One (`Ceiling-POE-VentIQ-RoomIQ`), the LED preview (`Ceiling-POE-VentIQ-RoomIQ-LED`, manifest-only under WF-LED-003), Rescue, the production-only `REQUIRED_CONFIGS = ["Ceiling-POE-VentIQ-RoomIQ", "Rescue"]`, the Release-One-only `scripts/data/kits.json`, the FanTRIAC HW-005 block, the WF-WIZARD-AVAIL-001 module-availability classifications, the WF-UX-002 readiness-string surface, the WF-LED-003 preview-channel acknowledgement gate, every firmware binary, `manifest.json`, every `firmware-*.json`, `firmware/sources.json`, every `.github/workflows/*` file, every test, `sw.js`, `_headers`, `index.html`, every CSS file, and every runtime JS file are byte-identical to pre-WF-IMPORT-GAP-001.
- **Stage 1 productized kit bundle presets (WF-KIT-PRESETS-001).** Step 1 leads with productized Sense360 kit bundle presets above the existing `[data-start-path]` path cards. The static markup at [`index.html`](index.html) (`[data-bundle-presets]` section) ships two installable cards — `S360-KIT-BATH-POE` (stable, badge `Recommended`, maps to `Ceiling-POE-VentIQ-RoomIQ`) and `S360-KIT-BATH-POE-LED` (preview, badge `Preview`, maps to `Ceiling-POE-VentIQ-RoomIQ-LED`) — plus a collapsible `<details data-bundle-presets-planned>` subsection with four planned fan-control bundles (Relay / TRIAC / PWM / DAC variants) that are intentionally non-installable. The preset list is a *static, local mirror* of upstream `sense360store/esphome-public` `KIT-MATRIX-001` and lives in [`scripts/data/kit-presets.js`](scripts/data/kit-presets.js) with helpers `findKitPresetById`, `isPresetMatch`, `isPresetAvailable`, `validateKitPreset`. The runtime controller at [`scripts/kit-presets.js`](scripts/kit-presets.js) wires each card: clicking an available card calls `setState({...cleared, ...preset.wizardState})`, records `setConfigurationMode('kit')` + `setSelectedKitSku(preset.id)` + `setActiveKitMetadata({...})` in [`scripts/services/diagnostics.js`](scripts/services/diagnostics.js), rewrites the URL to `?configmode=kit&preset=<id>`, and advances to `min(5, getMaxReachableStep())`. Clicking a planned card never mutates wizard state — it surfaces the `notAvailableReason` in the live region and the `[data-bundle-preset-availability-notice]` inline notice. The controller subscribes to `window.wizardStateSummary.onStateChange` so an exact state match flips a card to `aria-pressed="true"` + `is-active`, while a diverging state on the last-applied preset reads as `is-customized` with a "Customized from this bundle" inline label. **Installability stays manifest-driven and the preview preset still gates through `release-channels.js`** — the controller never imports `release-channels.js` and the LED preview build still requires the `channel:preview` acknowledgement at install time. The new bundle cards use the `data-bundle-preset-*` namespace so they do NOT collide with the existing `[data-preset-config]` quick-start preset surface in the kit panel. Test pins: `__tests__/kit-presets.test.js` (data-module integrity + manifest cross-check, preset-to-state mapping for both available bundles, URL hydration round-trip, planned-card no-mutation guarantee, live-notice surfacing, release-channel acknowledgement preservation, visual state reflection, and the manifest / sources / kits / `REQUIRED_CONFIGS` invariants). **Unchanged by WF-KIT-PRESETS-001:** every firmware binary, `manifest.json`, every `firmware-*.json`, `firmware/sources.json`, the `REQUIRED_CONFIGS = ["Ceiling-POE-VentIQ-RoomIQ", "Rescue"]` allowlist, `scripts/data/kits.json` (still Release-One-only — bundle presets are a *separate* presentation surface from the kit-config catalog), `scripts/data/module-requirements.js`, `scripts/utils/release-channels.js`, `scripts/utils/firmware-readiness.js`, `scripts/utils/module-availability.js`, `scripts/recommended-bundle.js`, `scripts/kit-mode.js`, every `.github/workflows/*` file, `sw.js` cache strategy / cache version (only the `SCRIPT_MODULES` allowlist gained the two new files), `_headers`, every Step 2-5 surface, the rescue modal, the WF-UX-006 path cards, the WF-LED-003 preview-channel acknowledgement model, the WF-TRIAC-001 advanced/manual-warning gate, and the FanTRIAC HW-005 block. `WF-KIT-LED-001` (the deferred decision to add an LED-bearing kit to `scripts/data/kits.json`) is **untouched**; bundle preset existence is presentation-only and never feeds back into the kit-config catalog, `REQUIRED_CONFIGS`, or any compliance / hardware-proof claim. No firmware imported. No LED-stable claim. No `RELEASE-007` / `S360-300-BENCH-001` / `WF-HW-TEST-003` completion claim.
- **TRIAC advanced/manual-warning runtime UX (WF-TRIAC-001).** `Sense360 TRIAC` (S360-320) is classified as `advanced-manual-warning` — the eighth availability state added to [`scripts/utils/module-availability.js`](scripts/utils/module-availability.js) (label **"Advanced / manual only"**, tone `danger`). The static override in [`scripts/data/module-requirements.js`](scripts/data/module-requirements.js) now reads `state: 'advanced-manual-warning'`, `reasonCode: 'hw-005-advanced-manual'`. The TRIAC card in [`index.html`](index.html) ships with the `is-advanced-warning` affordance (radio enabled, danger-tone styling — see [`css/wizard-style.css`](css/wizard-style.css) `.module-card.is-advanced-warning` and `.module-card__advanced-warning-*`) and a sibling `[data-advanced-warning-region]` warning + acknowledgement region that reveals only while TRIAC is the live selection. The region carries the **load-bearing copy** — *Sense360 TRIAC controls mains-connected loads*, *not compliance-certified*, *not Release-One*, *not a kit / default option*, *not recommended*, *no installable firmware has been imported yet*. The install gate (`updateFirmwareControls` in [`scripts/state.js`](scripts/state.js)) AND-s a new `advancedWarningAcksSatisfied` clause into `readyToFlash` and adds parallel tooltip / helper-text branches to every install-control block (Download .bin, Copy install link, esp-web-install activate button, mobile summary install). Ack plumbing: a session-only `advancedWarningAcknowledgements` `Map<string,true>` keyed by `${moduleKey}:${variantKey}`, with helpers `isAdvancedWarningAcknowledged` / `setAdvancedWarningAcknowledged` / `clearAdvancedWarningAck` / `clearAdvancedWarningAcks` / `getActiveAdvancedWarningSelections` / `getOutstandingAdvancedWarningAcknowledgements` / `pruneInactiveAdvancedWarningAcks` / `updateAdvancedWarningRegions` (all exposed via `__testHooks`). The ack is **orthogonal** to channel acknowledgements (preview / beta / development / deprecated); the WF-LED-003 preview acknowledgement model is unchanged. Deselecting TRIAC or switching to the kit path automatically drops the stored ack so a future reselection re-prompts. `classifyConfigString` was upgraded to return `advanced-manual-warning` (was `blocked`) for any FanTRIAC-token-bearing config; the legacy `blockedTokens` option name still resolves as a back-compat alias for `advancedWarningTokens`. Test pins: `__tests__/module-availability.test.js` (eight states, TRIAC override, load-bearing copy assertions, FanTRIAC-bearing config classified `advanced-manual-warning`); `__tests__/wizard-state.test.js` `WF-TRIAC-001 — advanced/manual-warning runtime integration` (radio not disabled, region reveal/hide, ack flow, install-gate defense-in-depth against a synthetic FanTRIAC manifest, deselection prunes ack, real-manifest no-build path, kit selection clears ack, LED preview path unaffected); `__tests__/a11y-static-html.test.js` `WF-TRIAC-001 — advanced/manual-warning region static markup` (single region scoped to fan=triac, hidden-by-default, load-bearing copy verbatim, ack checkbox shape, `aria-labelledby` wiring, TRIAC firmware-target hint copy); `__tests__/module-selection-guidance.test.js` `WF-TRIAC-001 — TRIAC card carries TRIAC-specific advanced/manual-warning firmware-target copy`. **Unchanged by WF-TRIAC-001:** every firmware binary, `manifest.json`, every `firmware-*.json`, `firmware/sources.json` (`block_tokens: ["FanTRIAC", "LED"]` on Release-One and `["FanTRIAC"]` on the LED preview both stand), `scripts/data/kits.json`, `scripts/utils/release-channels.js`, `scripts/utils/firmware-readiness.js` (six canonical readiness states unchanged — no seventh state introduced), `scripts/import-firmware-sources.py`, `scripts/gen-manifests.py`, `scripts/validate-product-import-readiness.js` (the catalog eligibility classifier still treats every FanTRIAC catalog entry as ineligible), `scripts/smoke-test-deployment.py`, `sw.js`, `_headers`, every `.github/workflows/*` file, `__tests__/fixtures/esphome-product-catalog.json`, the `REQUIRED_CONFIGS = ["Ceiling-POE-VentIQ-RoomIQ", "Rescue"]` allowlist, the WF-LED-003 preview-channel acknowledgement model, Release-One stable install path, LED preview install path, Rescue install path, the `scripts/recommended-bundle.js` `RECOMMENDED_STATE` (`fan: 'none'`), the WF-UX-006 path selector, the WF-UX-005 step labels. **FanTRIAC remains** not Release-One, not `REQUIRED_CONFIGS`, not kit / default, not recommended, not auto-selected, not import-allowed (the importer's `block_tokens` enforcement is unchanged), and not compliance-certified — advanced/manual-warning is an in-installer warning gate, **not** a compliance certification claim. The future `WF-IMPORT-TRIAC-001` follow-up still requires upstream `RELEASE-TRIAC-001`.
- **No internal IDs in customer-facing copy (WF-UX-008).** Internal engineering / task / release / tracking identifiers (`RELEASE-…-001`, `WF-IMPORT-…`, `WF-TRIAC-001`, `HW-005`, `COMPLIANCE-001`, `KIT-MATRIX-001`, `S360-300-BENCH-001`, `RELEASE-007`, `WF-HW-TEST-…`, internal doc filenames like `UPCOMING_PR.md`, internal script paths like `scripts/import-firmware-sources.py`) must **never** appear in customer-visible wizard copy. They are allowed only in (a) code comments, (b) developer/support-only data fields, and (c) machine-readable diagnostic codes. Concretely: in [`scripts/data/kit-presets.js`](scripts/data/kit-presets.js) the customer-facing fields are `displayName` / `shortName` / `description` / `badge` / `warning` / `notAvailableReason` / `components` (kept plain-language with a next step), while `upstreamRef` and `blockers` are the developer/support-only fields that carry the internal IDs and are **never rendered** (the [`scripts/kit-presets.js`](scripts/kit-presets.js) controller surfaces only `notAvailableReason` + `warning`, and nothing outside the data module consumes `upstreamRef` / `blockers`). In [`scripts/utils/module-availability.js`](scripts/utils/module-availability.js) the rendered `detail` prose (the `ADVANCED_MANUAL_WARNING_DETAIL` override + the `classifyConfigString` branch) is plain-language, and the internal mapping survives only in the machine `reasonCode` (`hw-005-advanced-manual`), which `state.js` never renders as text (it sets `data-availability-state`, not the reason). Every unavailable customer option must give **one plain-language reason + one suggested next step**. WF-UX-008 also reframed the Step 1 customer label **"Custom configuration" → "Advanced setup"** (the WF-UX-006 `data-start-path="custom"` / `configmode=custom` / `data-custom-path-panel` hooks and URL contract are unchanged — pure copy reframe) with the guidance *"Use this only if your hardware doesn't match a kit."* Guard tests: `__tests__/a11y-static-html.test.js` `WF-UX-008 — no internal task/release/tracking IDs in customer-facing copy` (bundle section / planned cards / path panels + the Advanced-setup copy pin) and `__tests__/kit-presets.test.js` (customer-facing-field ID guard + planned-reason next-step pin); the WF-TRIAC-001 `detail` assertions in `__tests__/module-availability.test.js` + `__tests__/wizard-state.test.js` now assert the IDs are absent from the rendered prose and present only in `reasonCode`. **Unchanged by WF-UX-008:** every firmware binary, `manifest.json`, every `firmware-*.json`, `firmware/sources.json`, `REQUIRED_CONFIGS`, `scripts/data/kits.json`, `scripts/data/module-requirements.js` field values, `scripts/utils/release-channels.js`, `scripts/utils/firmware-readiness.js`, the install gate / preflight / freshness engines, the WF-WIZARD-AVAIL-001 availability states + `reasonCode` values, the WF-TRIAC-001 acknowledgement gate, the WF-LED-003 preview-channel acknowledgement model, the FanTRIAC HW-005 import block, `sw.js`, `_headers`, and every `.github/workflows/*` file. Copy + test only; **no hardware behaviour was verified**.
- **First preview firmware batch (WF-PREVIEW-IMPORT-FIRST-BATCH-001).** Imported three preview-channel builds from upstream `sense360store/esphome-public` release [`v1.0.0-preview`](https://github.com/sense360store/esphome-public/releases/tag/v1.0.0-preview) — `Ceiling-POE-AirIQ-RoomIQ`, `Ceiling-POE-RoomIQ`, `Ceiling-POE-RoomIQ-LED` — via [`scripts/import-firmware-sources.py`](scripts/import-firmware-sources.py) (each `.bin` SHA-256-verified against the upstream `checksums-sha256.txt` **and** a pinned `expected_sha256`; provenance git sha `2228bbb7…`, ESPHome `2026.4.5`, compile run `26821900127` recorded in each `.meta.json`). Added three [`firmware/sources.json`](firmware/sources.json) entries (`channel: preview`; `block_tokens: ["FanTRIAC", "LED"]` for the two non-LED configs, `["FanTRIAC"]` for `Ceiling-POE-RoomIQ-LED`), staged three `.bin` + `.meta.json` sidecars, and regenerated `manifest.json` (3 → **6 builds**) + `firmware-*.json` (now `firmware-0…5.json`; **indices are not stable** — runtime resolves via `manifest.json` + `config_string`, nothing hardcodes a `firmware-N.json` index). AirIQ's static `no-firmware` override in [`scripts/utils/module-availability.js`](scripts/utils/module-availability.js) was **removed** so AirIQ derives `available-preview` from the manifest (the only runtime change; mirrors how LED is handled). The catalog fixture [`__tests__/fixtures/esphome-product-catalog.json`](__tests__/fixtures/esphome-product-catalog.json) gained the three real upstream `status: preview` rows; the alignment / readiness / availability / snapshot tests were rebaselined (6 builds, 5 sources, AirIQ `available-preview`). The three previews are **Advanced-install-only** behind the WF-LED-003 `channel:preview` acknowledgement — **firmware-build proof only**: not stable, not recommended, not a customer default, not hardware-verified, not buyable; never auto-selected (`preview.defaultSelectable: false`). The full import-proof record (assets, SHA256, sizes, commands, results) lives in [`docs/preview-import-first-batch-proof.md`](docs/preview-import-first-batch-proof.md). **Unchanged by WF-PREVIEW-IMPORT-FIRST-BATCH-001:** the `REQUIRED_CONFIGS = ["Ceiling-POE-VentIQ-RoomIQ", "Rescue"]` allowlist (production-only), `scripts/data/kits.json` (Release-One-only), the Release-One stable + VentIQ LED preview + Rescue build **content** (`sha256` / `md5` / `signature` / `signature_ed25519` / `file_size` byte-identical — only the generator-refreshed `source_commit` / `source_url` / `build_date` provenance fields differ), `scripts/utils/release-channels.js`, `scripts/utils/firmware-readiness.js`, the install gate / preflight / freshness engines, `scripts/data/kit-presets.js`, `scripts/data/module-requirements.js`, `sw.js`, `_headers`, `index.html`, every CSS file, and every `.github/workflows/*` file. The default **Simple install** path still resolves only to the stable Bathroom PoE build `Ceiling-POE-VentIQ-RoomIQ`. The fourth `v1.0.0-preview` asset (`Ceiling-POE-VentIQ-RoomIQ-LED`) was **not** re-imported — that config already ships from `v1.0.0-led-preview`, and re-importing the same-named file would overwrite a published `.bin`. **No TRIAC and no fan-driver (FanRelay / FanPWM / FanDAC) firmware imported. No LED-stable claim. No hardware / bench / compliance / commercial-availability proof claimed.**
- **FanRelay preview import (WEBFLASH-RELAY-001).** Imported the upstream `Ceiling-POE-VentIQ-FanRelay-RoomIQ` manual-preview build from `sense360store/esphome-public` [`v1.0.0-preview`](https://github.com/sense360store/esphome-public/releases/tag/v1.0.0-preview) (SHA256 `f9600a6b…d026ca4`, 989,840 bytes) as an Advanced-install-only preview. **Upstream two-concept eligibility model:** the import was authorised by upstream #711 (`RELEASE-PREVIEW-FAN-WEBFLASH-ELIGIBILITY-001`) setting `webflash_import_eligibility.eligible=true` in `config/preview-release-targets.json`, while the `product-catalog.json` lifecycle status stays `hardware-pending` and `webflash_build_matrix` stays `false` (no committed upstream build row — so `webflash_build_matrix: false` no longer means "preview import forbidden"; the downstream committed import is this WebFlash slice). WebFlash now recognises that flag for **import / manifest / kit** eligibility in [`__tests__/product-catalog-alignment.test.js`](__tests__/product-catalog-alignment.test.js) (`isWebflashImportEligible`) and [`scripts/validate-product-import-readiness.js`](scripts/validate-product-import-readiness.js) (the manual-preview lane + `PREVIEW_IMPORT_FIELDS`) — **never** for `REQUIRED_CONFIGS` (production-only) and **never** when `eligible !== true` (FanTRIAC stays `eligible: false` / rejected). Added one [`firmware/sources.json`](firmware/sources.json) entry (`channel: preview`, pinned `expected_sha256`, `block_tokens: ["FanTRIAC", "LED"]` — the FanRelay config carries neither token), staged the `.bin` + `.meta.json`, regenerated `manifest.json` (6 → **7 builds**) + `firmware-*.json` (**indices not stable**). `Sense360 Relay` (S360-310) moves from `design-pending` to `available-preview` in [`scripts/utils/module-availability.js`](scripts/utils/module-availability.js) via an explicit override carrying bespoke installer/developer-preview warning copy (plain-language, no internal IDs per WF-UX-008; the `design-pending` state stays in the taxonomy for any future schematic-less module); the declarative `availability` field in [`scripts/data/module-requirements.js`](scripts/data/module-requirements.js) is kept consistent. The catalog fixture [`__tests__/fixtures/esphome-product-catalog.json`](__tests__/fixtures/esphome-product-catalog.json) gained the real FanRelay row (`status: hardware-pending` + the `webflash_import_eligibility` object mirrored from `config/preview-release-targets.json`). Build-count / source-list test pins were rebaselined to 7 builds and FanRelay was dropped from the forbidden-fan-token guards (`live-smoke-preview-import`, `wf-ux-010…017`, `github-pages-surface`, `firmware-configurations-on-disk`, `sense360-webflash-status`). Full import-proof at [`docs/fanrelay-preview-import-proof.md`](docs/fanrelay-preview-import-proof.md). **Unchanged by WEBFLASH-RELAY-001:** `REQUIRED_CONFIGS = ["Ceiling-POE-VentIQ-RoomIQ", "Rescue"]` (production-only), `scripts/data/kits.json` (Release-One-only), `scripts/data/kit-presets.js` (no FanRelay card), the Release-One stable + first preview batch + VentIQ LED preview + Rescue build **content** (`sha256` / `md5` / `signature` / `signature_ed25519` / `file_size` byte-identical — only generator-refreshed provenance differs), `scripts/utils/release-channels.js`, `scripts/utils/firmware-readiness.js`, the install gate / preflight / freshness engines, `scripts/simple-install.js`, `sw.js`, `_headers`, `index.html`, every CSS file, every `.github/workflows/*` file, the FanTRIAC import block, the WF-LED-003 preview-channel acknowledgement model, and the WF-TRIAC-001 advanced/manual-warning gate. The default **Simple install** path still resolves only to the stable Bathroom PoE build `Ceiling-POE-VentIQ-RoomIQ`. **No FanPWM / FanDAC / FanTRIAC firmware imported** (FanPWM / FanDAC are upstream-import-eligible but reserved for `WF-IMPORT-PWM-001` / `WF-IMPORT-DAC-001`; FanTRIAC stays build-blocked). **No LED-stable claim. No hardware / bench / compliance / safety / commercial-availability proof claimed.**
- **FanPWM preview import (WEBFLASH-PWM-001).** Imported the upstream `Ceiling-POE-FanPWM` manual-preview build from `sense360store/esphome-public` [`v1.0.0-preview`](https://github.com/sense360store/esphome-public/releases/tag/v1.0.0-preview) (SHA256 `4ef9f353…c59926`, 950,720 bytes) as an Advanced-install-only preview — the direct sibling of WEBFLASH-RELAY-001 (#470). Same upstream two-concept eligibility model: authorised by upstream #711 (`RELEASE-PREVIEW-FAN-WEBFLASH-ELIGIBILITY-001`) setting `webflash_import_eligibility.eligible=true` in `config/preview-release-targets.json`, while the `product-catalog.json` lifecycle status stays `hardware-pending` and `webflash_build_matrix` stays `false`. The catalog-alignment guard ([`__tests__/product-catalog-alignment.test.js`](__tests__/product-catalog-alignment.test.js) `isWebflashImportEligible`) and the readiness validator ([`scripts/validate-product-import-readiness.js`](scripts/validate-product-import-readiness.js) manual-preview lane) already recognise that flag (from WEBFLASH-RELAY-001), so no validator change was needed — **import / manifest / kit** eligible, **never** `REQUIRED_CONFIGS` (production-only), **never** when `eligible !== true`. Added one [`firmware/sources.json`](firmware/sources.json) entry (`channel: preview`, pinned `expected_sha256`, `block_tokens: ["FanTRIAC", "LED"]` — the FanPWM config carries neither token), staged the `.bin` + `.meta.json`, regenerated `manifest.json` (7 → **8 builds**) + `firmware-*.json` (**indices not stable**). `Sense360 PWM` (S360-311) moves from `no-firmware` to `available-preview` in [`scripts/utils/module-availability.js`](scripts/utils/module-availability.js) via an explicit override carrying bespoke installer/developer-preview warning copy (low-voltage / DC fan control; plain-language, no internal IDs per WF-UX-008; the `no-firmware` state stays in the taxonomy for DAC); the declarative `availability` field in [`scripts/data/module-requirements.js`](scripts/data/module-requirements.js) is kept consistent. The catalog fixture [`__tests__/fixtures/esphome-product-catalog.json`](__tests__/fixtures/esphome-product-catalog.json) gained the real FanPWM row (`status: hardware-pending` + the `webflash_import_eligibility` object). Build-count / source-list test pins were rebaselined to 8 builds and FanPWM was dropped from the forbidden-fan-token guards (`live-smoke-preview-import`, `wf-ux-010…017`, `github-pages-surface`, `firmware-configurations-on-disk`, `sense360-webflash-status`). Full import-proof at [`docs/fanpwm-preview-import-proof.md`](docs/fanpwm-preview-import-proof.md). **Unchanged by WEBFLASH-PWM-001:** `REQUIRED_CONFIGS = ["Ceiling-POE-VentIQ-RoomIQ", "Rescue"]` (production-only), `scripts/data/kits.json` (Release-One-only), `scripts/data/kit-presets.js` (no FanPWM card), the Release-One stable + first preview batch + VentIQ LED preview + FanRelay preview + Rescue build **content** (`sha256` / `md5` / `signature` / `signature_ed25519` / `file_size` / `build_date` byte-identical — only generator-refreshed `source_commit` / `source_url` differ), `scripts/utils/release-channels.js`, `scripts/utils/firmware-readiness.js`, the install gate / preflight / freshness engines, `scripts/simple-install.js`, `sw.js`, `_headers`, `index.html`, every CSS file, every `.github/workflows/*` file, the FanTRIAC import block, the WF-LED-003 preview-channel acknowledgement model, and the WF-TRIAC-001 advanced/manual-warning gate. The default **Simple install** path still resolves only to the stable Bathroom PoE build `Ceiling-POE-VentIQ-RoomIQ`. **No FanDAC / FanTRIAC firmware imported** (FanDAC is upstream-import-eligible but reserved for `WF-IMPORT-DAC-001`; FanTRIAC stays build-blocked). **No LED-stable claim. No hardware / bench / compliance / safety / commercial-availability proof claimed.**
